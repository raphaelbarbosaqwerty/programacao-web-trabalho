<?php

    const DBUSER = 'root';
    const DBPASSWORD = 'root';
    const DBNAME = 'amiguinhos';
    const DBHOST = '172.18.0.2';

    // $connect = new PDO("mysql:host=172.18.0.2;port=3306;dbname=amiguinhos", 'root', 'root');

    // $connect->setAttribute(PDO::ATTR_EMULATE_PREPARES, false);
    // $connect->setAttribute(PDO::MYSQL_ATTR_USE_BUFFERED_QUERY, true);
    // $connect->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
    // define('APP_NAME', 'JORNAL DOS AMIGUINHOS');